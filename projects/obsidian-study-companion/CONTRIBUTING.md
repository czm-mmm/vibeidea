# 开发说明

使用 Node.js 24 LTS，在仓库根目录执行：

```bash
npm ci
npm test
npm run build
```

界面改动还应运行 README 中的 `npm run test:layout`，并在实际 Obsidian 中检查显示、输入和笔记切换。

提交源码、测试、文档与自拟示例；不要提交个人笔记、聊天记录、登录信息、`.obsidian` 配置、`node_modules`、`dist` 或构建压缩包。

学习建议与可靠参考解答要分开；不要将模型反馈或主观自评直接视为掌握证据。新增排程规则应解释适用条件与限制，并同步更新相关测试。
