# 参与贡献

感谢你愿意帮助改进 SCOUT · 马戏团。

## 报告问题

请优先使用仓库提供的 Issue 模板，并尽量写明设备与浏览器、复现步骤、预期结果、实际结果，以及必要的截图或录屏。涉及规则的问题，请同时说明人数、当前牌型、手牌顺序和采取的动作。

## 本地开发

```bash
npm install
npm run dev
```

提交修改前，请确保以下命令全部通过：

```bash
npm run typecheck
npm test
npm run build
```

## Pull Request

- 一个 PR 尽量只解决一个主题。
- 规则逻辑修改需要同步添加或更新测试。
- UI 修改请同时检查窄屏手机和桌面布局。
- 用户可见的变化请记录到 `CHANGELOG.md` 的 `Unreleased` 部分。
- 请勿提交 `dist/`、部署日志或构建压缩包。

提交信息建议使用简洁的中文动词开头，例如：`修正选牌方向`、`增加对局恢复`。
